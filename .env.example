# Copy this file to .env and fill in real values. Do NOT commit your .env to source control.

# Primary database connection (preferred)
# Use a full SQLAlchemy connection string. Replace placeholders with real values.
# If your password contains special characters (e.g. @, :, /), URL-encode it first
# (for example: password `p@ss:word` -> `p%40ss%3Aword`).
# Example (fill in your values):
# DATABASE_URL=postgresql+psycopg2://<DB_USER>:<DB_PASSWORD>@<DB_HOST>:5432/<DB_NAME>
DATABASE_URL=postgresql+psycopg2://your_username:your_password@localhost:5432/resume_customizer

# Optional: separate read-replica connection
# DB_READ_CONNECTION_STRING=postgresql+psycopg2://readonly:REPLACE_WITH_PASSWORD@replica-host:5432/resume_customizer

# Engine / pool settings (optional)
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=30
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_STATEMENT_TIMEOUT_MS=60000

# Log level for database module (DEBUG|INFO|WARNING|ERROR)
DB_LOG_LEVEL=INFO

# Encryption key for encrypting DB URLs (optional)
# FERNET_KEY=your_fernet_key_here